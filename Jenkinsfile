pipeline {
	agent any 
	stages {
		stage("welcome to jenkins") {
			steps {
				script {
					i =1
					while(i<=10){
					println "my i value is ${i}"
					sleep(5)
					}
					}
					}
				}
			}
		}
