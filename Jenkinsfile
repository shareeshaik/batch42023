 def myfn(a,b){
 println "welcome to functions"
 println "my value is ${a} & my value is ${b}"
 }

 pipeline {
	agent any 
	stages {
		stage("welcome to jenkins") {
			steps {
				script {
					//calling a function
					myfn(20,30)
					}
					}
				}
			}
		}
		 
